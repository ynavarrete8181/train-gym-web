import KardexMovimientos from "./components/KardexMovimientos";

const IndexKardexMovimientos = ({ usr_id }) => {
  return <KardexMovimientos usr_id={usr_id} />;
};

export default IndexKardexMovimientos;
